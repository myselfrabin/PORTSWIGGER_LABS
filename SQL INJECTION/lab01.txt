LAB 01: SQL injection vulnerability in WHERE clause allowing retrieval of hidden data

This lab contains a SQLI in a product category filter, when user select a category, the application
carries out a sql query in backend like this:

--> SELECT * FROM products WHERE category= Gifts' AND release=1

END GOAL: Perform a sqli attack and display the both release and unrelease product.